# Managed by homelab-tofu-workflow/scripts/enforce-pve-zfs-arc-cap-contract.sh
# Single-host Proxmox hypervisor: cap ARC below the old 6.2 GiB setting so
# guest-memory bursts do not recreate the Aug 2026 swap-thrash signature.
# 2026-09-07: 4 GiB -> 3 GiB. RCA: the host runs 50-52 GiB of guest RAM and
# sat chronically at the stall threshold (avail < 4 GiB AND swap > 2 GiB) for
# 24h+ because VM 201 (moltbot) runs 2 GiB over its declared 10240 contract
# (pending power-cycle). Trimmed ARC 1 GiB to buy back live headroom without
# a VM reboot while the VM 201 stop/start is coordinated. Reversible: restore
# 4294967296 to revert.
options zfs zfs_arc_max=3221225472
