# vm-gitops / OpenTofu state
.state/
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.*

# Override variables (contains secrets — never commit this)
terraform.tfvars
cloud-init/

# Non-secret migration phase flags must be tracked for CI parity.
!garage-migration.auto.tfvars
